# FastAPI Server Dependencies — Optimised for Render Free Tier (512 MB)
# Key changes vs. original:
#   - TensorFlow REMOVED  (saved ~700 MB) — TF models fall back to mock predictions
#   - torch CPU-only build (saved ~600 MB vs default CUDA build)
#   - opencv-python-headless (no X11/GUI libs needed in container)
#   - pytest / httpx removed (dev-only, not needed at runtime)

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Deep Learning — CPU-only PyTorch (installed via --extra-index-url in Dockerfile)
# torch and torchvision are installed in Dockerfile using the CPU index
# so we only pin versions here; the index override is in the Dockerfile
torch==2.1.0
torchvision==0.16.0

# Image Processing
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Audio Processing (Parkinson Detection)
librosa>=0.10.0
soundfile>=0.12.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# Utilities
python-jose>=3.3.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Scientific Computing
scikit-learn>=1.3.0

# Model utilities
timm>=0.9.0
joblib>=1.3.0
onnxruntime>=1.16.0
